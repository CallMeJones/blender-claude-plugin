schema_version = "1.0.0"

id = "claude_blender"
version = "0.1.0"
name = "Blender Agent Bridge"
tagline = "Scene-aware bridge for Claude, Codex, and MCP agents"
maintainer = "Michael"
type = "add-on"
blender_version_min = "5.1.0"
license = [
  "SPDX:GPL-3.0-or-later",
]

[permissions]
network = "Call Anthropic/docs; expose optional localhost MCP bridge"
files = "Docs, captures, checkpoints, transcripts, and audit logs"
