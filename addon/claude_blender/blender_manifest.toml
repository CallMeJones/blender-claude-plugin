schema_version = "1.0.0"

id = "claude_blender"
version = "0.1.0"
name = "Blender Agent Bridge"
tagline = "Safe scene bridge for external AI agents"
maintainer = "Michael"
type = "add-on"
blender_version_min = "5.1.0"
license = [
  "SPDX:GPL-3.0-or-later",
]

[permissions]
network = "Fetch Blender docs; expose optional localhost MCP bridge"
files = "Docs, captures, checkpoints, transcripts, and audit logs"
